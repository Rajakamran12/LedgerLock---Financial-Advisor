GOOGLE_GENERATIVE_AI_API_KEY=
CHAT_MODEL=gemini-flash-latest
EMBEDDING_MODEL=gemini-embedding-001
AGENT_API_KEY=
# max requests per minute per client, enforced by the in-memory token bucket
RATE_LIMIT_PER_MINUTE=30
